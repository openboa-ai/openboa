# PR Pages

Create one `PR-*.md` page per active or landed frontier.

Use `.agents/templates/pr-page.md` as the starting point.

Every PR page should explain the current quality target, the current quality gap, and why the PR is not ready yet.
