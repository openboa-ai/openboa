# Run Pages

Create one `RUN-*.md` page per iteration inside a PR.

Use `.agents/templates/run-page.md` as the starting point.

Every run page should make the targeted quality axis and net quality delta explicit.
