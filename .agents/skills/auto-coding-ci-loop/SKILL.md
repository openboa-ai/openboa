---
name: auto-coding-ci-loop
description: CI follow-through specialist for project harness coding work. Use inside `auto-coding` when local verification is green and CI evidence is needed before a PR can move toward promotion.
---

# Auto Coding CI Loop

Use when:

- local evidence is green
- CI must confirm correctness
- CI failure needs bounded diagnosis

If the fix is out of scope, hand back immediately.
