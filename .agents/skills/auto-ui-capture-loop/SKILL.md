---
name: auto-ui-capture-loop
description: Live capture specialist for project harness UI work. Use inside `auto-ui` when the worker must launch the real surface and create comparable baseline and after screenshots.
---

# Auto UI Capture Loop

Minimum capture set:

- desktop baseline
- mobile baseline when responsive
- any state that proves the defect

Capture before editing and after editing.
