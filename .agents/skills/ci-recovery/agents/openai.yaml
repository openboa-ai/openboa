interface:
  display_name: "CI Recovery"
  short_description: "Inspect and recover failing CI"
  icon_small: "./assets/github-small.svg"
  icon_large: "./assets/github.png"
  default_prompt: "Inspect failing GitHub Actions checks in this repo, summarize the actionable root cause, and return a bounded recovery packet."
