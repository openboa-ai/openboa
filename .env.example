# openboa environment example
# Copy to `.env` and fill only the values you use.

# Gateway auth token (required when binding beyond loopback)
# OPENBOA_GATEWAY_TOKEN=change-me-to-a-long-random-token

# At least one model provider key is expected when model execution is enabled.
# OPENAI_API_KEY=
# ANTHROPIC_API_KEY=

